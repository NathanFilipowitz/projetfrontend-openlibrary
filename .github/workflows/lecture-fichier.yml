name: Lecture de Fichier
on:
  push:
    branches: [main]
jobs:
  verification:
    runs-on: ubuntu-latest
    steps:
    # Étape indispensable pour accéder à vos fichiers
    - name: Récupération du code
      uses: actions/checkout@v3
    - name: Afficher le contenu du dossier
      run: ls -la
    # - name: Lire le message
    #   run: 